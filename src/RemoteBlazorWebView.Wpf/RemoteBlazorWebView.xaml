<UserControl x:Class="RemoteBlazorWebView.Wpf.RemoteBlazorWebView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RemoteBlazorWebView.Wpf"
             xmlns:base="clr-namespace:PeakSWC;assembly=PeakSWC.RemoteableWebView" mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800"  >
   
    <Grid x:Name="MainGrid"  >

        <TextBlock Visibility="{Binding Path=ShowHyperlink, UpdateSourceTrigger=PropertyChanged}">
            
            <Hyperlink Click="Hyperlink_Click">
             
                <TextBlock Text="{Binding Path=Uri, UpdateSourceTrigger=PropertyChanged}" />
             </Hyperlink>
        </TextBlock>
       
        
        <base:RemoteBlazorWebViewBase Visibility="{Binding Path=ShowWebWindow, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" x:Name="MainBlazorWebView">
       
        </base:RemoteBlazorWebViewBase>


    </Grid>
</UserControl>
